# Flask (Python) backend environment variables (copy to .env and edit)
FLASK_ENV=development
PORT=3000
JWT_SECRET=change_this_secret

# SMTP (optional)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="IT Support <itsupport@company.com>"
